package com.luxmap.core.theme

import androidx.compose.ui.graphics.Color

val Navy = Color(0xFF1F3864) // Primary — thanh điều hướng, nút chính
val Blue = Color(0xFF3E86C9) // Secondary — liên kết, icon
val Teal = Color(0xFF5FC4B0) // Accent — hoàn thành, xác nhận
val LightBlue = Color(0xFF8FD9E8) // Nền khối thông tin
val Warn = Color(0xFFE9A23B) // Sắp đến hạn
val Danger = Color(0xFFD64545) // Quá hạn, lỗi, xoá
val Bg = Color(0xFFF5F7FA)
val TextPrimary = Color(0xFF1A1A1A)
val TextSecondary = Color(0xFF6B7280)

// Badge trạng thái (bg / text) — dùng đúng cặp, không đổi
val StatusReceivedBg = Color(0xFFE8EEF7)
val StatusReceivedText = Color(0xFF1F3864)
val StatusProgressBg = Color(0xFFFFF3DC)
val StatusProgressText = Color(0xFF8A5A00)
val StatusDoneBg = Color(0xFFE3F6F1)
val StatusDoneText = Color(0xFF1E6B5C)
val StatusOverdueBg = Color(0xFFFBE4E4)
val StatusOverdueText = Color(0xFF9B2C2C)
val StatusRejectedBg = Color(0xFFEFEFEF)
val StatusRejectedText = Color(0xFF555555)
