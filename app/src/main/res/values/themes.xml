<resources>
    <!-- Chỉ dùng cho splash/launch trước khi Compose vẽ; theme thật nằm ở core/theme/Theme.kt -->
    <style name="Theme.LuxMap" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/luxmap_navy</item>
    </style>
</resources>
