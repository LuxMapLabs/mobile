<?xml version="1.0" encoding="utf-8"?>
<!--
  Applies only to the debug build (app/src/debug). The dev backend runs plain HTTP, no TLS
  (see launchSettings.json). Cleartext is allowed only for these hosts used during
  development — this does NOT apply to the release build.
  - 10.0.2.2: Android Emulator reaching the host machine
  - localhost: real device with "adb reverse"
  - 192.168.1.50: real device over Wi-Fi (dev machine LAN IP, change it if the IP changes)
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">192.168.1.50</domain>
    </domain-config>
</network-security-config>
