<?xml version="1.0" encoding="utf-8"?>
<!--
  Chỉ áp dụng cho build debug (app/src/debug). Backend dev chạy HTTP thường, không TLS
  (xem launchSettings.json). Chỉ mở cleartext cho đúng 2 host loopback dùng khi phát triển —
  KHÔNG áp dụng cho build release.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
